1. instal  npm init
2. install package npm i bcryptjs cookie-parser cors dotenv express ioredis jsonwebtoken mongoose ts-node-dev 
@types/bcryptjs @types/cookie-parser @types/cors @types/expres @types/jsonwebtoken @types/node typescript
3. create konfigurasi app.ts  dan server ts
4. create dev di package json
5. create body parser dan  cors origin
6. konfigurasi database dan redis
7. konfigurasi errorHandler dan asyncError
8. npx tsc --init //create tsconfig.ts


2. npm i ejs --save-dev @types/ejs untuk  view engine yang menggunakan sintaks JavaScript untuk menghasilkan tampilan.
3. npm i nodemailer --save-dev @types/nodemailer for email with ts

1. git branch nama-branch
2. git add
3. git commit
4. git status
5. git checkout buat pindah brancg
5. git branch liat brach aktif

# //kalo mau gabungkan brach
1. chekcout ke branch utama
2. git pull origin main
3. git merge --no-ff  nama_branch
4. git push origin main
5. git branch -D nama_branch //delete brach aktif jika tidak di butuhkan
6. git push origin --delete nama_branch //delete file bracng github

# change branch
1. git branch -m nama_branch_baru # « jika sedang berada di branch yang akan diganti nama
2. git push origin -d branch_lama #delete branch lama di github
3. git push origin branch_baru  #add branch baru to github